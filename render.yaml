services:
  - type: web
    name: sitec-wordpress
    runtime: docker
    plan: starter
    envVars:
      - key: DB_HOST
        fromService:
          type: pserv
          name: sitec-mysql
          property: host
      - key: DB_PORT
        fromService:
          type: pserv
          name: sitec-mysql
          property: port
      - key: DB_NAME
        value: wordpress
      - key: DB_USER
        value: wordpress
      - key: DB_PASSWORD
        generateValue: true
      - key: WP_HOME
        value: https://sitec-wordpress.onrender.com
      - key: WP_SITEURL
        value: https://sitec-wordpress.onrender.com
      - key: DISABLE_WP_CRON
        value: "1"
      - key: FORCE_SSL_ADMIN
        value: "1"

  - type: pserv
    name: sitec-mysql
    env: docker
    plan: starter
    dockerCommand: >-
      bash -lc "docker-entrypoint.py mysqld"
    repo: https://github.com/render-examples/mysql
    branch: main
    envVars:
      - key: MYSQL_DATABASE
        value: wordpress
      - key: MYSQL_USER
        value: wordpress
      - key: MYSQL_PASSWORD
        generateValue: true
      - key: MYSQL_ROOT_PASSWORD
        generateValue: true
    disk:
      name: mysql-data
      mountPath: /var/lib/mysql
      sizeGB: 10
